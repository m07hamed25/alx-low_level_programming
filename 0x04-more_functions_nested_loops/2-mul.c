#include "main.h"

/**
 * mul - Multiplies two integers
 * @x: the frist number to multiply
 * @y: the second number to multiply
 * Return: the result of multiplication between x and y
 */

int mul(int x, int y)
{
	return (x * y);
}
