#include "main.h"

/**
 * factorial - Calculate the factorial of a number
 * @n: the number to claculate
 *
 * Return: Integer value
 */

int factorial(int n)
{
	for (int i = 1; i <= n; i++)
		return (n *= i);
}
